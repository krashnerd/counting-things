
def solve_recur(factors, coeff):
	if(len(factors)==1):
		return factors.coeff(coeff)
	for term in factors:


def solve(factors, coeff):
	ans = 0
	used_coeff = coeff

	for poly in range(len(factors)):
		for term in poly:

	





